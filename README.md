# --FPGA--single-MLP-
基于FPGA实现简单单层MLP手写图像识别：
基本原理：
训练端
输入一张28x28的黑底白字图像（在代码中已经检测过，如果是白底黑字，需要手动反转）
经过三层卷积层和池化层，提取特征，经过反向传播后得到weight矩阵和bias矩阵

推理端
利用dprom提前输入bias矩阵和weight矩阵，存入只读全局存储器，等待调用
利用dpram读入提前生成好的图像binary文件
在读入的时候连续和每一个数字的weight矩阵相乘,加上bias矩阵
在输入后的连续10个clk中输出每一个数字的最终结果
最后比较出最大值后，亮起对应的led模块
